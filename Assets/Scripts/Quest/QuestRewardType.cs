namespace TCG.Quest
{
    public enum QuestRewardType
    {
        Currency, // Gold, Gems, or Shards
        Item,     // Any ItemData (card, pack, cosmetic)
        XP        // Reserved for a future player-level system
    }
}
